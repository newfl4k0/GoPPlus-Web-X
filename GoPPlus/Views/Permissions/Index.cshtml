@model IEnumerable<GoPS.Models.Permissions>
@using GoPS.Helpers;
@{
    ViewBag.Title = "Permisos";
}
@Html.Hidden("Permiso", "General")
<div class="main">
    @{Html.RenderPartial("NavBar_CatGenerales", "Catálogo de Permisos"); }
    <div class="row">
        <div class="col-lg-12">
            <br />
            <p>
                @Html.ActionLink("Nuevo", "Create", null, htmlAttributes: new { @class = "btn btn-default _Edicion" })
            </p>
            @if (ViewBag.Delete != null)
            {
                <p class="text-success">Registro Eliminado Exitosamente</p>
            }
            <div class="panel panel-default">
                <div class="panel-body">
                    <table data-toggle="table" data-pagination="true" data-show-refresh="false" data-show-toggle="true" data-show-columns="true" data-search="true" data-select-item-name="toolbar1" data-sort-name="Nombre" data-sort-order="asc">
                        <thead>
                            <tr>
                                <th data-field="Nombre" data-sortable="true" data-sorter="customSorter">
                                    @Html.Label("Permiso")
                                </th>
                                <th data-field="Description" data-sortable="true" data-sorter="customSorter">
                                    @Html.Label("Descripción")
                                </th>
                                <th data-field="Able" data-sortable="true" data-sorter="customSorter">
                                    @Html.Label("Habilitado")
                                </th>
                                @*<th data-field="AspNetRoles.Name">
                                    @Html.Label("Perfil")
                                </th>*@
                                <th>@Html.Label("Acciones")</th>
                            </tr>
                        </thead>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Able)
                                </td>
                                @*<td>
                                    @Html.DisplayFor(modelItem => item.AspNetRoles.Name)
                                </td>*@
                                <td class="div_actions">
                                    @Html.EncodedActionLink("Editar", "Edit", "Permissions", new { id = item.Id }, new { @class = "_Edicion" })<span class="_Edicion pipe"> | </span>
                                    @Html.EncodedActionLink("Detalles", "Details","Permissions", new { id = item.Id }, new { @class = "_Visualizacion" })<span class="_Visualizacion pipe"> | </span>
                                    @Html.EncodedActionLink("Eliminar", "Delete", "Permissions", new { id = item.Id }, new { @class = "_Edicion" })
                                </td>
                            </tr>
                        }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
