@model GoPS.Models.MensajesPredefinidos
@using GoPS.Helpers;
@{
    ViewBag.Title = "Ver Detalles de Mensaje Predefinido";
}

@Html.Hidden("Permiso", "Configuraciones")
<div class="main">
    @{Html.RenderPartial("Breadcrumb", "Mensajes Predefinidos > Detalles"); }
    <div class="row">
        <div class="col-lg-12">
            <div>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.Texto)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Texto)
                    </dd>
                </dl>
            </div>
            <p class="div_actions">
                @Html.EncodedActionLink("Editar", "Edit", "MensajesPredefinidos", new { id = Model.ID_MensajePredefinido }, new { @class = "_Edicion" })<span class="_Edicion pipe"> | </span>
                @Html.ActionLink("Regresar", "Index", null, new { @class = "_Visualizacion" })
            </p>
        </div>
    </div>
</div>