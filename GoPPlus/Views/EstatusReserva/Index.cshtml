@model IEnumerable<GoPS.Models.Estatus_Reserva>

@{
    ViewBag.Title = "Estatus Reservas";
}

@Html.Hidden("Permiso", "Configuraciones")

<div class="main">
    @{Html.RenderPartial("NavBar_CatConfiguraciones", "Catálogo de Estatus Reservas"); }
    <div class="row">
        <div class="col-lg-12">
            <br />
            <p>
                @Html.ActionLink("Nuevo", "Create", null, htmlAttributes: new { @class = "btn btn-default" })
            </p>
            @if (ViewBag.Delete != null)
            {
                <p class="text-success">Registro Eliminado Exitosamente</p>
            }
            <div class="panel panel-default">
                <div class="panel-body">
                    <table data-toggle="table" data-pagination="true" data-show-refresh="false" data-show-toggle="true" data-show-columns="true" data-search="true" data-select-item-name="toolbar1" data-sort-name="Nombre" data-sort-order="asc">
                        <thead>
                            <tr>
                                <th data-field="Nombre" data-sortable="true" data-sorter="customSorter">
                                    @Html.Label("Estatus")
                                </th>
                                <th data-field="Descripcion" data-sortable="true" data-sorter="customSorter">
                                    @Html.Label("Descripción")
                                </th>
                                <th data-field="Orden" data-sortable="true" data-sorter="numberSorter">
                                    @Html.Label("Orden")
                                </th>
                                <th>@Html.Label("Acciones")</th>
                            </tr>
                        </thead>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Nombre)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Descripcion)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Orden)
                                </td>
                                <td class="div_actions">
                                    @Html.ActionLink("Editar", "Edit", new { id = item.ID_Estatus_Reserva }, new { @class = "_Edicion" })<span class="_Edicion pipe"> | </span>
                                    @Html.ActionLink("Detalles", "Details", new { id = item.ID_Estatus_Reserva }, new { @class = "_Visualizacion" })<span class="_Visualizacion pipe"> | </span>
                                    @Html.ActionLink("Eliminar", "Delete", new { id = item.ID_Estatus_Reserva }, new { @class = "_Edicion" })
                                </td>
                            </tr>
                        }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>